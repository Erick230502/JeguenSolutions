<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Jeguen Solutions</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
<style>
    html, body {
        margin: 0;
        padding: 0;
        width: 100%;
        overflow-x: hidden;
    }

    :root {
        --primary-color: #1a4f8a;
        --secondary-color: #ff9900; 
        --light-text-color: #ffffff; 
        --dark-text-color: #333333; 
        --background-light: #f8f9fa; 
        --border-radius: 8px;
        --transition-speed: 0.3s ease-in-out;
        --dark-blue-overlay: rgba(13, 70, 124, 0.7);
    }

    body {
        font-family: "Montserrat", Arial, sans-serif;
        box-sizing: border-box;
        color: var(--dark-text-color);
        background-color: white;
    }

    *, *::before, *::after {
        box-sizing: inherit;
    }

    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 15px;
    }

    .btn {
        display: inline-block;
        padding: 12px 25px;
        border-radius: var(--border-radius);
        text-decoration: none;
        font-weight: 600;
        transition: background-color var(--transition-speed), color var(--transition-speed);
    }

    .btn-primary {
        background-color: var(--primary-color);
        color: var(--light-text-color);
        border: 1px solid var(--primary-color);
    }

    .btn-primary:hover {
        background-color: #153c69; 
        border-color: #153c69;
    }

    .btn-secondary {
        background-color: transparent;
        color: var(--primary-color);
        border: 1px solid var(--primary-color);
    }

    .btn-secondary:hover {
        background-color: var(--primary-color);
        color: var(--light-text-color);
    }

    .mt-4 { margin-top: 1.5rem; }
    .mt-5 { margin-top: 3rem; }
    .mb-4 { margin-bottom: 1.5rem; }
    .text-center { text-align: center; }


/* Hero Section */
.hero-section {
    background: linear-gradient(var(--dark-blue-overlay), var(--dark-blue-overlay)),
                url('/img/trabajando11.jpg') no-repeat center center/cover;
    color: var(--light-text-color);
    padding: 6rem 0;
    text-align: center;
    display: flex;
    /* align-items: center; /* REMUEVE O CAMBIA ESTO */
    align-items: flex-start; /* CAMBIO CLAVE: Alinea el contenido arriba */
    justify-content: center;
    min-height: 100vh;
    position: relative;
    overflow: hidden;
}

.hero-content {
    position: relative;
    z-index: 2;
    padding-top: 15vh; 
}

.hero-content h1 {
    font-size: 4rem;
    margin-bottom: 0.2rem;
    margin-top: 0; 
    color: var(--light-text-color);
    font-weight: 450;
    text-transform: uppercase;
    line-height: 1.1;
}

    .hero-content .hero-tagline {
        font-size: 2.3rem;
        max-width: 900px;
        margin: 0 auto;
        line-height: 1.5;
        color: rgba(255, 255, 255, 0.9);
        font-weight: 350;
        text-transform: uppercase;
    }

    /* Section Titles - Generalizado */
    .section-title {
        font-size: 3rem;
        margin-bottom: 3rem;
        position: relative;
        display: block; 
        margin-left: auto; 
        margin-right: auto; 
        color: var(--primary-color);
        font-weight: 500;
        text-transform: uppercase;
        text-align: center; 
    }

    .section-title::after {
        content: '';
        position: absolute;
        left: 50%;
        transform: translateX(-50%);
        bottom: -10px;
        width: 80px;
        height: 4px;
        background-color: var(--secondary-color);
        border-radius: 2px;
    }

/* About Us Section - Nuevo Diseño con Imagen Full-Width y Degradado a la Izquierda */
.about-us-section {
    position: relative;
    width: 100%;
    min-height: 700px; 
    display: flex;
    align-items: center;
    overflow: hidden;
    padding: 0;
}

.about-us-section .background-image {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: 1;
    filter: grayscale(10%);
}

.about-content-wrapper {
    position: relative;
    z-index: 2;
    width: 100%;
    display: flex;
    justify-content: flex-start; 
    padding: 3rem 15px;
    align-items: center;
    min-height: inherit;
}

.about-text-box {
    background: linear-gradient(to bottom, rgba(255, 255, 255, 0.95), rgba(255, 255, 255, 0.7));
    border-radius: var(--border-radius);
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
    padding: 3rem; 
    width: 50vw;
    max-width: 800px;
    min-height: 800px; 
    color: var(--dark-text-color);
    position: absolute; 
    left: 0; 
    top: 0;
    border-top-right-radius:0; 
    border-bottom-right-radius: 0; 
    border-top-left-radius: 0; 
    border-bottom-left-radius: 0; 
    padding-top: 6rem; 
}

/* Estilos específicos para el título "Nosotros" dentro de about-text-box */
.about-text-box h2.section-title {
    margin-left:8.2rem;
    margin-top: 0;
    margin-bottom: 2rem; 
    
}
.about-text-box h2.section-title::after {
    margin-left:-8.2rem;
    right: 0;
    transform: translateX(0); 
}

.about-text-box p {
    font-size: 1.1rem;
    margin-bottom: 1rem;
    line-height: 1.8;
    max-width: 80%; 
    margin-left: auto; 
    text-align: justify; 
}

    /* Philosophy Section - Misión, Visión, Valores con Imágenes */
    .philosophy-section {
        padding: 5rem 0;
        background-color: white; /* Asegurado que el fondo es blanco */
    }

    .philosophy-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 2rem;
        margin-top: 3rem;
    }

    .philosophy-card {
        background-color: var(--background-light);
        border-radius: var(--border-radius);
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.08);
        padding: 2rem;
        text-align: center;
        transition: transform var(--transition-speed), box-shadow var(--transition-speed);
        height: 100%;
        display: flex;
        flex-direction: column;
        justify-content: flex-start;
        overflow: hidden; 
    }

    .philosophy-card:hover {
        transform: translateY(-8px) scale(1.02); 
        box-shadow: 0 10px 55px rgba(0, 0, 0, 0.4); 
    }

    .philosophy-card .card-image {
        width: 100%;
        height: 200px; 
        object-fit: cover; 
        border-radius: var(--border-radius) var(--border-radius) 0 0; 
        margin-bottom: 1.5rem; 
    }

    .section-title-philosophy{
        margin-top:-50px;
        font-size: 3rem;
        margin-bottom: 2rem;
        position: relative;
        display: block;
        margin-left: auto; 
        margin-right: auto; 
        color: var(--primary-color);
        font-weight: 500;
        text-transform: uppercase;
        text-align: center; 
    }

     .section-title-philosophy::after {
        content: '';
        position: absolute;
        left: 50%;
        transform: translateX(-50%);
        bottom: -10px;
        width: 80px;
        height: 4px;
        background-color: var(--secondary-color);
        border-radius: 2px;
    }

    .philosophy-card h3 {
        font-size: 1.8rem;
        color: var(--primary-color);
        margin-bottom: 1rem;
        position: relative;
        display: inline-block;
    }

    .philosophy-card h3::after {
        content: '';
        position: absolute;
        left: 50%;
        transform: translateX(-50%);
        bottom: -5px;
        width: 50px;
        height: 3px;
        background-color: var(--secondary-color);
        border-radius: 1.5px;
    }

    .philosophy-card ul {
        list-style: none;
        padding: 0;
        margin: 0;
        text-align: left;
    }

    .philosophy-card ul li {
        font-size: 1rem;
        color: #555;
        margin-bottom: 0.5rem;
        padding-left: 1.5rem;
        position: relative;
    }

    .philosophy-card ul li::before {
        content: "\f058"; 
        font-family: "Font Awesome 5 Free";
        font-weight: 900;
        color: var(--primary-color);
        position: absolute;
        left: 0;
        top: 0;
        line-height: inherit;
    }

/* Commitment Section */
.commitment-section {
    background-color: var(--primary-color);
    padding: 1.5rem 0;
    text-align: center;
    margin-bottom: 30px;
    margin-top: -50px;
    color: var(--light-text-color);
    display: flex;
    align-items: center;
    justify-content: center;
}

.commitment-title {
    font-size: 2.5rem;
    color: var(--light-text-color);
    margin-bottom: 1.5rem;
    font-weight: 450;
    text-transform: uppercase;
}

.commitment-grid {
    display: flex;
    justify-content: center;
    align-items: center;
    flex-wrap: wrap; /* Esto es crucial para que los ítems se envuelvan a la siguiente línea */
    gap: 0.8rem;
    font-size: 1.1rem;
    font-weight: 500;
    padding: 0 1rem; /* Añade un poco de padding horizontal para evitar que toque los bordes en móviles */
}

.commitment-item {
    background-color: transparent;
    border-radius: 0;
    padding: 0;
    font-size: inherit;
    font-weight: inherit;
    color: var(--light-text-color);
    transition: none;
    display: flex;
    align-items: center;
    justify-content: center;
    white-space: nowrap; /* Mantiene el texto del ítem en una sola línea */
}

.commitment-item span {
    margin: 0;
}

.commitment-item:hover {
    background-color: transparent;
    color: var(--light-text-color);
    transform: none;
    cursor: default;
}

.separator {
    color: rgba(255, 255, 255, 0.5);
    font-size: 1.2rem;
    font-weight: bold;
    padding: 0 0.5rem; /* Reducimos el padding horizontal del separador */
    line-height: 1;
    /* Considera que el separador puede no ser necesario si los elementos se apilan */
    /* Para pantallas muy pequeñas, podríamos ocultarlo o ajustarlo más */
}

/* Estilos para la sección de servicios (general) */
.services-overview-section {
    padding: 4rem 0;
}

/* Estilos para el contenedor del carrusel */
.services-carousel {
    width: 100%;
    margin-top: 3rem; 
}

/* Estilos para el párrafo introductorio de servicios */
.services-intro {
    text-align: justify; 
    margin-bottom: 2rem; 
    max-width: 1100px; 
    margin-left: auto; 
    margin-right: auto; 
    padding: 0 15px; 
}

/* Estilos para cada slide/tarjeta de servicio */
.section-title-services{
        margin-top: 100px;
        font-size: 3rem;
        margin-bottom: 2rem;
        position: relative;
        display: block;
        margin-left: auto; 
        margin-right: auto; 
        color: var(--primary-color);
        font-weight: 500;
        text-transform: uppercase;
        text-align: center; 
    }

     .section-title-services::after {
        content: '';
        position: absolute;
        left: 50%;
        transform: translateX(-50%);
        bottom: -10px;
        width: 80px;
        height: 4px;
        background-color: var(--secondary-color);
        border-radius: 2px;
    }

/* Estilos para cada slide/tarjeta de servicio */
.service-slide {
    position: relative;
    overflow: hidden;
    border-radius: var(--border-radius);
    box-shadow: 0 6px 12px rgba(0, 0, 0, 0.1);
    height: 250px; 
    font-size: 1rem;
    display: flex;
    align-items: flex-end; 
    justify-content: center; 
    text-align: justify;
    cursor: pointer;
    transition: transform var(--transition-speed), box-shadow var(--transition-speed);
}

/* NUEVO ESTILO: Para el enlace que envuelve la tarjeta */
.service-slide-link {
    position: absolute; 
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    text-decoration: none;
    color: inherit; 
    flex-direction: column;
    justify-content: flex-end; 
    align-items: center; 
    transition: transform var(--transition-speed), box-shadow var(--transition-speed);
}

.service-slide:hover .service-slide-link {
    transform: translateY(-5px); 
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15); 
}

/* Estilos para la imagen dentro del slide (dentro del enlace ahora) */
.service-slide-img {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: brightness(0.7);
    transition: filter var(--transition-speed), transform var(--transition-speed); 
}

.service-slide:hover .service-slide-img {
    filter: brightness(0.5); /* Oscurece más la imagen al hacer hover */
    transform: scale(1.05); /* Zoom ligero en la imagen al hacer hover */
}

/* Estilos para la capa de superposición con el texto (nombre de la categoría) */
.service-slide-overlay {
    position: relative;
    z-index: 2; 
    width: 100%;
    padding: 1rem 1.5rem 1.5rem 1.5rem; 
    background: linear-gradient(to top, rgba(0, 0, 0, 0.8), rgba(0, 0, 0, 0));
    color: var(--light-text-color);
    font-weight: 600;
    text-transform: uppercase;
    font-size: 1.1rem; 
    height: auto; 
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-end;
    text-align: center;
    transition: background var(--transition-speed);
}

.service-slide-overlay h3 {
    margin: 0;
    color: inherit;
    padding-bottom: 0.2rem;
    line-height: 1.2;
}

/* Estilos para los botones de navegación de Swiper */
.swiper-button-next,
.swiper-button-prev {
    color: var(--primary-color) !important; 
    background-color: rgba(255, 255, 255, 0.7); 
    border-radius: 50%;
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 2px 5px rgba(0,0,0,0.2);
    transition: background-color var(--transition-speed);
}

.swiper-button-next:hover,
.swiper-button-prev:hover {
    background-color: var(--secondary-color); 
    color: white !important;
}

.swiper-button-next::after,
.swiper-button-prev::after {
    font-size: 1.5rem !important; 
}

/* Estilos para la paginación (los puntos inferiores) */
.swiper-pagination-bullet {
    background-color: rgba(var(--primary-color-rgb), 0.5) !important; 
    opacity: 1 !important;
}

.swiper-pagination-bullet-active {
    background-color: var(--primary-color) !important;
}

/* Clients Section */
.clients-section {
    padding: 5rem 0;
    background-color: var(--background-blue-section); /* Fondo azul claro */
}

.section-title-clients{
        margin-top:-80px;
        font-size: 2.5rem;
        margin-bottom: 1rem;
        position: relative;
        display: block;
        margin-left: auto; 
        margin-right: auto; 
        color: var(--primary-color);
        font-weight: 700;
        text-transform: uppercase;
        text-align: center; 
    }

     .section-title-clients::after {
        content: '';
        position: absolute;
        left: 50%;
        transform: translateX(-50%);
        bottom: -10px;
        width: 80px;
        height: 4px;
        background-color: var(--secondary-color);
        border-radius: 2px;
    }

.clients-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); /* Ajusta los logos al espacio */
    gap: 2rem;
    justify-items: center; /* Centra los logos en sus celdas */
    align-items: center;
    margin-top: 3rem;
}

.clients-grid img {
    max-width: 100%;
    height: 80px; /* Altura fija para los logos, ajusta según necesites */
    object-fit: contain; /* Asegura que la imagen se adapte sin cortarse */
    filter: grayscale(100%) opacity(70%); /* Logos en escala de grises y semitransparentes */
    transition: filter var(--transition-speed), transform var(--transition-speed);
}

.clients-grid img:hover {
    filter: grayscale(0%) opacity(100%); /* Color al pasar el ratón */
    transform: scale(1.05);
}

/* Contact Section */
.contact-section {
    padding: 6rem 0; /* Más espacio vertical */
    background-color: #ffffff; /* Fondo blanco puro */
    text-align: center; /* Centrar el título de la sección */
}

/* Título de la sección de contacto */
.section-title {
    font-size: 3rem;
    margin-bottom: 2rem;
    position: relative;
    display: inline-block;
    color: var(--primary-color);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 1px;
}

.section-title::after {
    content: '';
    position: absolute;
    left: 50%;
    transform: translateX(-50%);
    bottom: -15px;
    width: 100px;
    height: 5px;
    background-color: var(--secondary-color);
    border-radius: 3px;
}

.contact-grid {
    display: grid; /* Cambiado a grid para dos columnas */
    grid-template-columns: 1fr 1fr; /* Una columna para info, otra para imagen */
    gap: 3rem; /* Espacio entre las columnas */
    margin-top: 4rem; /* Más espacio entre el título y el contenido */
    padding: 0 1.5rem; /* Padding horizontal para evitar que se pegue a los bordes */
    align-items: center; /* Centra verticalmente el contenido de las columnas */
    max-width: 1200px; /* Limita el ancho de la cuadrícula */
    margin-left: auto; /* Centra la cuadrícula */
    margin-right: auto; /* Centra la cuadrícula */
}

.contact-info-area {
    background-color: #ffffff; /* Fondo blanco para la tarjeta de contacto */
    padding: 3rem; /* Más padding interno */
    border-radius: var(--border-radius);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1); /* Sombra más pronunciada para efecto de tarjeta */
    text-align: left; /* Alineación del texto dentro de la tarjeta */
    width: 100%; /* Ocupa el ancho disponible en la columna */
    transition: transform var(--transition-speed), box-shadow var(--transition-speed);
}

.contact-info-area:hover {
    transform: translateY(-5px); /* Efecto sutil al pasar el ratón */
    box-shadow: 0 15px 40px rgba(0, 0, 0, 0.15); /* Sombra más fuerte al pasar el ratón */
}

.contact-info-area h3 {
    font-size: 2.2rem; /* Títulos un poco más grandes dentro de la tarjeta */
    color: var(--primary-color);
    margin-bottom: 1.5rem; /* Más espacio debajo de los títulos */
    text-align: center; /* Centrar títulos dentro de la tarjeta */
}

/* Estilo para el segundo h3 (Horario de Atención) */
.contact-info-area h3.mt-4 {
    margin-top: 2.5rem; /* Aumenta el margen superior */
    margin-bottom: 1rem;
}

.contact-info-area p {
    font-size: 1.1rem; /* Texto más grande para la información */
    color: var(--text-color);
    margin-bottom: 0.8rem; /* Espacio entre líneas de información */
    line-height: 1.5;
    display: flex; /* Para alinear el icono y el texto */
    align-items: center; /* Centrar verticalmente el icono con el texto */
}

.contact-info-area p .fas {
    color: var(--secondary-color); /* Color para los iconos */
    margin-right: 1rem; /* Espacio entre el icono y el texto */
    font-size: 1.3rem; /* Tamaño del icono */
    min-width: 25px; /* Asegura un ancho mínimo para los iconos */
}

.contact-info-area .contact-phone {
    font-size: 1.8rem; 
    font-weight: 700;
    color: var(--primary-color);
    margin-top: 1rem;
    text-align: center; 
}

/* Estilos para la nueva área de imagen de contacto */
.contact-image-area {
    display: flex;
    justify-content: center;
    align-items: center;
    overflow: hidden; 
    border-radius: var(--border-radius); 
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1); 
}

.contact-image {
    width: 100%;
    height: auto; 
    display: block; 
    object-fit: cover; 
    border-radius: var(--border-radius); 
}


/* --- Media Queries para index.ejs (Responsividad del contenido) --- */
@media (max-width: 992px) {
    .hero-content h1 {
        font-size: 3.2rem;
    }
    .hero-content .hero-tagline {
        font-size: 1.6rem;
    }
    .section-title {
        font-size: 2rem;
    }

    .about-text-box {
        width: 60%; 
        padding: 2.5rem;

    }
    .about-text-box h2.section-title {
        /* Puedes ajustar los márgenes para el título si la caja es más estrecha */
        margin-left: 5rem; /* Ejemplo, ajústalo según el diseño */
    }
    .about-text-box h2.section-title::after {
        margin-left: -5rem; /* Ejemplo, ajústalo según el diseño */
    }
    .about-text-box p {
        font-size: 1.05rem; /* Ligeramente más pequeño que el escritorio */
        max-width: 90%; /* Ajusta el ancho del párrafo */
    }

    .service-card {
        padding: 2rem;
    }
    .service-card .icon-large {
        font-size: 3rem;
    }
    .philosophy-card h3 {
        font-size: 1.6rem;
    }
    .philosophy-card .card-image {
        height: 180px;
    }
    /* Ajustes para la sección de compromiso */
    .commitment-title {
        font-size: 2rem; /* Reduce el tamaño del título en tablets */
    }
    .commitment-grid {
        font-size: 1rem; /* Reduce el tamaño de fuente de los ítems en tablets */
        gap: 0.6rem; /* Ajusta el espacio entre ítems */
    }
    .separator {
        padding: 0 0.4rem; /* Reduce aún más el padding del separador */
    }
}

@media (max-width: 768px) {
    .hero-section {
        padding: 6rem 0;
        min-height: 60vh;
    }
    .hero-content h1 {
        font-size: 2.5rem;
    }
    .hero-content .hero-tagline {
        font-size: 1.2rem;
    }
    .section-title {
        font-size: 1.8rem;
        margin-bottom: 2rem;
    }
    /* Ajuste para about-us-section en móvil */
    .about-us-section {
        min-height: auto; /* Permite que la altura se ajuste al contenido en móvil */
        padding-top: 3rem; /* Añade un poco de espacio arriba si es necesario */
        padding-bottom: 3rem; /* Añade un poco de espacio abajo si es necesario */
    }
    .about-content-wrapper {
        justify-content: center;
        padding: 2rem 15px;
        align-items: flex-start; /* Asegura que el texto inicie arriba */
    }
    .about-text-box {
        width: 90%;
        max-width: none;
        padding: 2rem;
        text-align: center;
        position: relative; /* Muy importante para que no se posicione absolutamente */
        left: unset; /* Reinicia la posición izquierda */
        top: unset; /* Reinicia la posición superior */
        min-height: auto; /* La altura se ajusta al contenido */
        border-radius: var(--border-radius); /* Asegura esquinas redondeadas */
        /* Los border-radius específicos se eliminan para que tome el valor por defecto */
        border-top-right-radius: var(--border-radius);
        border-bottom-right-radius: var(--border-radius);
        border-top-left-radius: var(--border-radius);
        border-bottom-left-radius: var(--border-radius);
        padding-top: 2rem; /* Ajusta el padding top para que sea consistente con el resto del padding */
    }
    .about-text-box h2.section-title {
        text-align: center; /* Centra el título en móvil */
        margin-left: 0; /* Elimina el margen izquierdo específico */
        font-size: 1.6rem; /* Ajusta el tamaño de fuente del título para 768px */
    }
    .about-text-box h2.section-title::after {
        left: 50%; /* Centra la línea del título en móvil */
        transform: translateX(-50%);
        margin-left: auto; /* Asegura que no tenga un margin-left negativo de antes */
        margin-right: auto;
    }
    .about-text-box p {
        font-size: 1rem; /* Reduce el tamaño de fuente de los párrafos */
        max-width: 100%; /* Los párrafos ocupan todo el ancho disponible */
        margin-left: 0; /* Elimina el margen izquierdo automático */
        text-align: justify; /* Mantiene la justificación si es deseado, o cámbialo a 'left' */
    }

    .philosophy-grid {
        grid-template-columns: 1fr;
    }
    .philosophy-card {
        width: 90%;
        margin: 0 auto;
    }
    .philosophy-card h3::after {
        left: 50%;
        transform: translateX(-50%);
    }
    .philosophy-card ul {
        text-align: left;
    }
    .service-cards-grid {
        grid-template-columns: 1fr;
    }
    .service-card {
        width: 90%;
        margin: 0 auto;
    }
    .about-us-section, .services-overview-section, .cta-section, .philosophy-section {
        padding: 3rem 0;
    }
    .cta-section h2 {
        font-size: 2rem;
    }
    .cta-section p {
        font-size: 1rem;
    }
    .philosophy-card .card-image {
        height: 150px;
    }
    /* Ajustes para la sección de compromiso */
    .commitment-section {
        padding: 1rem 0; /* Reduce el padding vertical de la sección */
        margin-top: -30px; /* Ajusta el margen superior si es necesario */
    }
    .commitment-title {
        font-size: 1.8rem; /* Reduce el tamaño del título en móviles */
        margin-bottom: 1rem;
    }
    .commitment-grid {
        font-size: 0.95rem; /* Reduce el tamaño de fuente de los ítems */
        gap: 0.5rem; /* Reduce el espacio entre ítems */
        /* Flexbox ya está manejando el wrap */
    }
    .separator {
        padding: 0 0.3rem; /* Menos padding para el separador */
        font-size: 1rem; /* Reduce el tamaño del separador */
    }
}

@media (max-width: 480px) {
    .hero-content h1 {
        font-size: 2rem;
    }
    .hero-content .hero-tagline {
        font-size: 1rem;
    }
    .section-title {
        font-size: 1.6rem;
    }
    .philosophy-card h3 {
        font-size: 1.4rem;
    }
    .philosophy-card ul li {
        font-size: 0.9rem;
    }
    .about-text-box {
        padding: 1.5rem; 
    }
    /* Ajustes para la sección de compromiso en los teléfonos más pequeños */
    .commitment-title {
        font-size: 1.5rem; /* Aún más pequeño para pantallas muy chicas */
        margin-bottom: 0.8rem;
    }
    .commitment-grid {
        font-size: 0.9rem; /* Tamaño de fuente más pequeño para los ítems */
        gap: 0.4rem; /* Espacio mínimo entre ítems */
    }
    .separator {
        padding: 0 0.2rem; /* Separa los ítems lo mínimo indispensable */
        font-size: 0.9rem; /* Separádor más pequeño */
        /* Opcional: Si quieres que cada item y separador se apile individualmente en pantallas muy pequeñas,
           puedes considerar display: block; para .commitment-item y .separator aquí,
           pero la forma actual con flex-wrap debería ser suficiente para la mayoría de los casos.
           Si se ve muy amontonado, podrías incluso ocultar el separador si los ítems se apilan:
           display: none;
        */
    }
}
</style>
</head>
<body>

<section id="servicios" class="services-overview-section container" data-aos="fade-up" data-aos-delay="100">
    <h2 class="section-title-services text-center">SERVICIOS</h2>
    <p class="services-intro text-center">En JEGUEN SOLUTIONS, ofrecemos higiene profunda y desinfección total con métodos avanzados y personal certificado. Transformamos tus espacios en ambientes verdaderamente saludables y productivos, asegurando un bienestar que notarás y querrás mantener.</p>

    <div class="swiper mySwiper services-carousel">
        <div class="swiper-wrapper">
            <a href="/limpieza" class="swiper-slide service-slide">
                <img src="/img/asesoria2.jpeg" alt="Asesoría" class="service-slide-img">
                <div class="service-slide-overlay">
                    <h3>Productos de limpieza</h3>
                </div>
            </a>
            <a href="/limpieza" class="swiper-slide service-slide">
                <img src="/img/serviciosLim.jpeg" alt="Instalación" class="service-slide-img">
                <div class="service-slide-overlay">
                    <h3>Servicios de limpieza</h3>
                </div>
            </a>
            <a href="/limpieza" class="swiper-slide service-slide">
                <img src="/img/mantenimiento.jpeg" alt="Mantenimiento" class="service-slide-img">
                <div class="service-slide-overlay">
                    <h3>Mantenimiento</h3>
                </div>
            </a>
            <a href="/limpieza" class="swiper-slide service-slide">
                <img src="/img/fumigacion.jpeg" alt="Diseño y desarrollo de sistemas" class="service-slide-img">
                <div class="service-slide-overlay">
                    <h3>Fumigación</h3>
                </div>
            </a>
            <a href="/limpieza" class="swiper-slide service-slide">
                <img src="/img/limInf.jpeg" alt="Suministro de equipo" class="service-slide-img">
                <div class="service-slide-overlay">
                    <h3>Limpieza y desinfección</h3>
                </div>
            </a>
        </div>

        <div class="swiper-button-next"></div>
        <div class="swiper-button-prev"></div>
    </div>
</section>

 <section id="inicio" class="hero-section" data-aos="fade-up">
    <div class="hero-content container">
        <h1 data-aos="fade-right" data-aos-delay="200">SOLUCIONES QUE TRANSFORMAN.</h1>
        <p class="hero-tagline" data-aos="fade-left" data-aos-delay="400">RESULTADOS QUE MARCAN LA DIFERENCIA.</p>
    </div>
</section>

<section id="nosotros" class="about-us-section" data-aos="fade-up" data-aos-delay="100">
    <img src="/img/nosotros1.jpg" alt="Equipo de Jeguen Solutions" class="background-image">
    <div class="about-content-wrapper">
        <div class="about-text-box" data-aos="fade-right" data-aos-delay="200">
            <h2 class="section-title">Nosotros</h2>
            <p>En JEGUEN SOLUTION, nos especializamos en ofrecer soluciones profesionales de limpieza, adaptadas a las necesidades de cada cliente.</p>
            <p>Combinamos experiencia, compromiso y productos de alta calidad para garantizar espacios seguros y saludables.</p>
            <p>Nuestro equipo está capacitado para brindar un servicio confiable, puntual y eficiente, con atención al detalle y un enfoque orientado a la satisfacción del cliente.</p>
        </div>
    </div>
</section>

<section id="filosofia" class="philosophy-section container" data-aos="fade-up" data-aos-delay="200">
    <h2 class="section-title-philosophy text-center">Filosofía</h2>
    <div class="philosophy-grid">
        <div class="philosophy-card"> <img src="/img/mision.jpg" alt="Misión" class="card-image">
            <h3>Misión</h3>
            <ul>
                <li>Brindar soluciones integrales de limpieza con productos de calidad y servicios profesionales, garantizando ambientes limpios, seguros y saludables.</li>
            </ul>
        </div>
        <div class="philosophy-card"> <img src="/img/vision.jpg" alt="Visión" class="card-image">
            <h3>Visión</h3>
            <ul>
                <li>Ser la empresa líder en limpieza profesional reconocida por su excelencia, innovación y compromiso con el bienestar de nuestros clientes.</li>
            </ul>
        </div>
        <div class="philosophy-card"> <img src="/img/valores.jpg" alt="Valores" class="card-image">
            <h3>Valores</h3>
            <ul>
                <li>Innovación: Mejoramos continuamente nuestros procesos y soluciones.</li>
                <li>Responsabilidad: Cumplimos con compromiso y seriedad cada tarea.</li>
                <li>Calidad: Ofrecemos productos y servicios que superan las expectativas.</li>
                <li>Confianza: Actuamos con honestidad y transparencia.</li>
                <li>Sostenibilidad: Promovemos prácticas que cuidan el medio ambiente.</li>
            </ul>
        </div>
    </div>
</section>

<section class="commitment-section" data-aos="fade-up">
    <div class="container">
        <h2 class="commitment-title text-center">COMPROMETIDOS CON LA:</h2>
        <div class="commitment-grid">
            <div class="commitment-item" data-aos="zoom-in" data-aos-delay="100">
                <span>Innovación</span>
            </div>
            <div class="separator" data-aos="zoom-in" data-aos-delay="150">|</div> <div class="commitment-item" data-aos="zoom-in" data-aos-delay="200">
                <span>Tecnología</span>
            </div>
            <div class="separator" data-aos="zoom-in" data-aos-delay="250">|</div> <div class="commitment-item" data-aos="zoom-in" data-aos-delay="300">
                <span>Vanguardia</span>
            </div>
            <div class="separator" data-aos="zoom-in" data-aos-delay="350">|</div> <div class="commitment-item" data-aos="zoom-in" data-aos-delay="400">
                <span>Seguridad</span>
            </div>
        </div>
    </div>
</section>

<section class="clients-section container" data-aos="fade-up" data-aos-delay="100">
    <h2 class="section-title-clients text-center">CLIENTES</h2>
    <div class="clients-grid">
        <img src="/img/SEDENA.png" alt="Logo Cliente SEDENA" loading="lazy">
        <img src="/img/DIF HIDALGO.jpeg" alt="Logo Cliente DIF Hidalgo" loading="lazy">
        <img src="/img/LogoDegasa.png" alt="Logo Cliente Bodega Aurrera" loading="lazy">
        <img src="/img/Hospital infantil Mexico.jpg" alt="Logo Cliente Unidades Médicas" loading="lazy">
        <img src="/img/medpol.png" alt="Logo Cliente Medpol" loading="lazy">
        <img src="/img/INPer_logo.jpg" alt="Logo Cliente Infer" loading="lazy">
        <img src="/img/SEYPRO.png" alt="Logo Cliente Instituto Potosino" loading="lazy">
        <img src="/img/DIF MTY.jpeg" alt="Logo Cliente DIF Monterrey" loading="lazy">
        </div>
</section>

<section id="contacto" class="contact-section container" data-aos="fade-up" data-aos-delay="100">
    <h2 class="section-title text-center">CONTÁCTANOS</h2>
    <div class="contact-grid">
        <div class="contact-info-area">
            <h3>Información de Contacto</h3>
            <p><i class="fas fa-map-marker-alt"></i> Ozumba, Estado de México, CP. 56800, México</p>
            <p><i class="fas fa-phone-alt"></i> Teléfono: 55 54625545</p>
            <p><i class="fas fa-envelope"></i> Email: administrativo@jeguen.com </p>
            
            <h3 class="mt-4">Horario de Atención</h3>
            <p>Lunes a Viernes: 9:00 AM - 6:00 PM</p>
            <p>Sábado: 9:00 AM - 1:00 PM</p>
        </div>
        <!-- Nueva sección para la imagen -->
        <div class="contact-image-area">
            <img src="/img/nosotros.jpg" alt="Imagen de Contacto Jeguen Solutions" class="contact-image">
        </div>
    </div>
</section>


<script src="[https://unpkg.com/aos@2.3.1/dist/aos.js](https://unpkg.com/aos@2.3.1/dist/aos.js)"></script>
<script>
    AOS.init();
</script>

<script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
<script>
// Asegúrate de que el DOM esté cargado antes de inicializar Swiper
document.addEventListener('DOMContentLoaded', function() {
    var swiper = new Swiper(".mySwiper", {
        slidesPerView: 1, // Muestra 1 slide por vista en móvil
        spaceBetween: 30, // Espacio entre slides
        loop: true, // Carrusel infinito
        grabCursor: true, // Cambia el cursor para indicar que se puede arrastrar
        pagination: {
            el: ".swiper-pagination",
            clickable: true,
        },
        navigation: {
            nextEl: ".swiper-button-next",
            prevEl: ".swiper-button-prev",
        },
        autoplay: {
            delay: 4000, // Cambia de slide cada 4 segundos
            disableOnInteraction: false, // El autoplay no se detiene si el usuario interactúa
        },
        breakpoints: {
            // Cuando la pantalla sea >= 768px
            768: {
                slidesPerView: 3, // Muestra 3 slides por vista
                spaceBetween: 30,
            },
            // Cuando la pantalla sea >= 1024px
            1024: {
                slidesPerView: 4, // Muestra 4 slides por vista
                spaceBetween: 40,
            },
            // Cuando la pantalla sea >= 1200px
            1200: {
                slidesPerView: 5, // Muestra 5 slides por vista
                spaceBetween: 50,
            }
        },
        // Integración con AOS
        on: {
            slideChangeTransitionEnd: function () {
                AOS.refresh(); // Refresca AOS después de cada transición de slide
            }
        }
    });
});
</script>

</body>
</html>